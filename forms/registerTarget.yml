loadbalancers: Select the loadbalancer. For TargetType as alb, loadbalancer must have at least one listener that matches the target group port.
ip: Enter IP address.
hosts: Select hosts.
lambdas: Select the Lambda function(s) you want to register.